//10.3
import { templates, select } from '../settings.js';

class DatePicker {

  //10.2.2 Add Class Booking with constructor,  

  constructor(datePickerElement) {
    //stała
    const thisDatePicker = this;
    //
    thisWidget.dom.input = thisWidget.dom.wrapper(`input[name="date"]`);
    
    //init methods render and initWidgets 10.2.3

    thisDatePicker.initPlugin();
    thisDatePicker.initWidgets();
  
  }
  initWidgets() {
    const thisDatePicker = this;
    
  }


}
